# Portal Backend

## Setup

1. Copy `.env.example` to `.env` and adjust values if needed.
2. Install dependencies: `npm install`
3. Start server: `npm run dev`

Magic link emails are written to `backend/outbox` as text files.
